import { Image } from '../../../templates/helpers/image';

const data = {
  text: 'Card title die vrij lang is waardoor die op meerdere regels komt te staan',
  url: '/',
  imageUrls: Image('Card', 0)
};

export default data;